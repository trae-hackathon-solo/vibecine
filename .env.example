# Client-side direct PixVerse calls (required — copy same value as PIXVERSE_API_KEY)
NEXT_PUBLIC_PIXVERSE_API_KEY=your_pixverse_api_key

# Server-only (optional, legacy API routes)
PIXVERSE_API_KEY=your_pixverse_api_key

# Optional AI storyboard via server route
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
